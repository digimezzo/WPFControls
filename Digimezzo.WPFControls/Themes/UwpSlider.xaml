<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Digimezzo.WPFControls">

    <Style TargetType="{x:Type local:HorizontalUWPSlider}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HorizontalUWPSlider}">
                    <Grid>
                        <Canvas x:Name="PART_CanvasHelper" Panel.ZIndex="0" Height="18" Visibility="Hidden" Margin="0,0,18,0"/>
                        <!-- Background="#00000000" is required to IsMouseOver is detected -->
                        <Canvas x:Name="PART_Canvas" Panel.ZIndex="1" Background="#00000000" Height="18" Width="{TemplateBinding ActualWidth}">
                            <Rectangle x:Name="PART_Track" Canvas.Right="0" Canvas.Top="8" Panel.ZIndex="0" Height="2" Width="{Binding Path=TrackLength,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Fill="{Binding Path=TrackBackground,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle x:Name="PART_Bar" Canvas.Left="0" Canvas.Top="8" Panel.ZIndex="0" Height="2" Width="{Binding Path=BarLength,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Fill="{Binding Path=BarBackground,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Button x:Name="PART_Button" Canvas.Top="0" Panel.ZIndex="1" Height="18" Width="18" Margin="0" Canvas.Left="{Binding Path=Position,RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" BorderThickness="0">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type Button}">
                                                    <Border x:Name="PART_Border" Margin="0" Background="#00000000" BorderThickness="2" BorderBrush="{Binding Path=ButtonBackground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:HorizontalUWPSlider}}}" CornerRadius="9"/>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="true">
                                                            <Setter Property="Cursor" Value="Hand"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="PART_Border" Property="Background" Value="{Binding Path=ButtonBackground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:HorizontalUWPSlider}}}"/>
                                                            <Setter TargetName="PART_Border" Property="BorderThickness" Value="0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Canvas>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:VerticalUWPSlider}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:VerticalUWPSlider}">
                    <Grid>
                        <Canvas x:Name="PART_CanvasHelper" Panel.ZIndex="0" Width="18" Visibility="Hidden" Margin="0,18,0,0"/>
                        <!-- Background="#00000000" is required to IsMouseOver is detected -->
                        <Canvas x:Name="PART_Canvas" Panel.ZIndex="1" Background="#00000000" Height="{TemplateBinding ActualHeight}" Width="18">
                            <Rectangle x:Name="PART_Track" Canvas.Left="8" Canvas.Top="0" Panel.ZIndex="0" Height="{Binding Path=TrackLength,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Width="2" Fill="{Binding Path=TrackBackground,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle x:Name="PART_Bar" Canvas.Left="8" Canvas.Bottom="0" Panel.ZIndex="0" Height="{Binding Path=BarLength,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Width="2" Fill="{Binding Path=BarBackground,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Button x:Name="PART_Button" Canvas.Left="0" Panel.ZIndex="1" Height="18" Width="18" Margin="0" Canvas.Bottom="{Binding Path=Position,RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" BorderThickness="0">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type Button}">
                                                    <Border x:Name="PART_Border" Margin="0" Background="#00000000" BorderThickness="2" BorderBrush="{Binding Path=ButtonBackground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:VerticalUWPSlider}}}" CornerRadius="9"/>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="true">
                                                            <Setter Property="Cursor" Value="Hand"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="PART_Border" Property="Background" Value="{Binding Path=ButtonBackground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:VerticalUWPSlider}}}"/>
                                                            <Setter TargetName="PART_Border" Property="BorderThickness" Value="0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Canvas>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:HorizontalUWPBottomSlider}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HorizontalUWPBottomSlider}">
                    <Grid>
                        <Border x:Name="PART_BorderHelper" Visibility="Hidden" CornerRadius="9,9,9,0"/>
                        <Canvas x:Name="PART_CanvasHelper" Panel.ZIndex="0" Height="18" Visibility="Hidden" Margin="0,0,18,0"/>
                        <!-- Background="#00000000" is required to IsMouseOver is detected -->
                        <Canvas x:Name="PART_Canvas" Panel.ZIndex="1" Background="#00000000" Height="18" Width="{TemplateBinding ActualWidth}">
                            <Rectangle x:Name="PART_Track" Canvas.Right="0" Canvas.Top="16" Panel.ZIndex="0" Height="2" Width="{Binding Path=TrackLength,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Fill="{Binding Path=TrackBackground,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle x:Name="PART_Bar" Canvas.Left="0" Canvas.Top="16" Panel.ZIndex="0" Height="2" Width="{Binding Path=BarLength,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Fill="{Binding Path=BarBackground,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Button x:Name="PART_Button" Canvas.Top="0" Panel.ZIndex="1" Height="18" Width="18" Margin="0" Canvas.Left="{Binding Path=Position,RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" BorderThickness="0">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type Button}">
                                                    <Border x:Name="PART_Border" Margin="0" Background="#00000000" BorderThickness="2" BorderBrush="{Binding Path=ButtonBackground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:HorizontalUWPBottomSlider}}}" CornerRadius="{Binding Path=CornerRadius,ElementName=PART_BorderHelper}"/>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="true">
                                                            <Setter Property="Cursor" Value="Hand"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="PART_Border" Property="Background" Value="{Binding Path=ButtonBackground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:HorizontalUWPBottomSlider}}}"/>
                                                            <Setter TargetName="PART_Border" Property="BorderThickness" Value="0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Canvas>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
